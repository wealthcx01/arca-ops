# library/ — what the work produced

Outputs and artefacts: analyses, models, dashboards, runbooks, board packs. The durable record of
what was actually made, as opposed to `context/`, which is what was known going in.

Heavy binaries (video, large images, raw exports) belong in object storage with a pointer file here —
git is for the things worth diffing.
