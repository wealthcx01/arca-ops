# docs/tickets/ — the Scale queue

One markdown file per ticket. A ticket whose Status field reads `Todo` may be picked up by the lane on its next wake;
`Planned` means it is backlog and will be left alone. The Foundry Studio renders this directory as
ARCA's Scale board.
