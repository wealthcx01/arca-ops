# docs/tickets/ — the Scale queue

One markdown file per ticket. `**Status:** Todo` means the lane may pick it up on its next wake;
`Planned` means it is backlog and will be left alone. The Foundry Studio renders this directory as
ARCA's Scale board.
